
@model List<Semester1Project.Models.Job>
@{
    ViewBag.Title = "Index";
}
@using Semester1Project.Models;
@helper RenderJob(Job job) { 
    }
@if(Session["UserId"] != null)
{
   
    @Html.Partial("IndexMap")
    foreach(Job job in Model) { 
     @Html.Partial("IndexJobBanner",job)
    }
}


