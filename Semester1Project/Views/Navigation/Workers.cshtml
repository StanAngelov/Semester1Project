@model IEnumerable<Semester1Project.Models.User>

@{
    ViewBag.Title = "Workers";
}

<div class="titlebar">Hired workers</div>

@foreach (var item in Model)
{
    @Html.Partial("UserBanner", item)
}
