@model Semester1Project.ViewModels.UserApplicationViewModel
<div class="BannerRoot">
    <a href="@Url.Action("UserDetail/"+Model.Applicant.UserId)">
        <div class="row jobrow">
            <div class="col-xs-9 jobbanner">
                <span class="bannerSpanTitle">@Model.Applicant.UserName </span>
                <span class="bannerSpanLocation">@Model.Applicant.FullName </span>
            </div>
            <div class="col-xs-3 jobpayment">
                <span class="bannerPayment">@Model.Applicant.Email</span>
                <br />
                <span>@Model.Applicant.PhoneNum</span>
            </div>
        </div>
    </a>
</div>
